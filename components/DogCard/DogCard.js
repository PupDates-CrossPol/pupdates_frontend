import * as React from 'react';
import { StyleSheet, Text, View, Image, TouchableOpacity} from 'react-native';

const DogInfo = ({dogPack}) => {

    return (
        <View> 
            <Text>THIS IS WHERE THE DOGS GO!!</Text>
        </View>
        )

}

export default DogInfo